Haan bhai bilkul sahi hai! 🎯

**Logic clean hai:**

```
First time ever:
  → clone repo
  → create branch "kaptaan" (permanent, ek baar)

Har naye task se pehle:
  → git fetch origin
  → git merge origin/main (ya rebase)
  → ab kaam shuru

Kaam ke dauran:
  → git commit karte raho freely

Task khatam:
  → final commit
  → done (no PR, no merge to main)
```

**Fayde:**
- Tu khud decide karega kab main mein merge karna hai
- Agent ka kaam sirf code likhna + commit karna
- Branch hamesha fresh start main se sync hokar aata hai
- `open_pr` aur `merge_pr` tools — hata do, confusion hi hai

---

**Dono changes combined — Replit ke liye instructions:**

---

**Change 1: E2B Sandbox Pause/Resume**

In `internal/sandbox/e2b.go` and `internal/agent/agent.go`:

- Sandbox create karte waqt `lifecycle.onTimeout = "pause"` set karo
- `sandboxId` ko DB mein store karo — `memories` table mein key `_sandbox_id` use kar lo (underscore prefix = internal)
- `ensureSandbox()` logic change karo:
  - Pehle DB se `_sandbox_id` check karo
  - Mila to `Sandbox.connect(sandboxId)` karo (auto-resume if paused)
  - Nahi mila to naya create karo, ID store karo
- `reset_sandbox` tool mein: `sandbox.kill()` karo + DB se `_sandbox_id` delete karo

**Change 2: Git Lifecycle Simplify**

In `internal/agent/agent.go` system prompt aur `ensureSandbox()`:

- Branch name: `kaptaan` — fixed, timestamp nahi
- Sandbox create hote waqt:
  ```
  git clone <repo>
  cd workspace
  git checkout kaptaan 2>/dev/null || git checkout -b kaptaan
  ```
- Har naye task ke start pe (agent turn 1):
  ```
  git fetch origin
  git merge origin/main --no-edit
  ```
- System prompt mein sirf yahi rakho git ke liye:
  - Commit karte raho meaningful chunks pe
  - Branch hamesha `kaptaan` hai
  - `open_pr` aur `merge_pr` mat use karo

**Remove karo:**
- `open_pr` tool — delete definition + handler
- `merge_pr` tool — delete definition + handler
- System prompt se saari PR/merge instructions

**Do not touch:** SSE, auth, chat feed, memories, UI

---

Ye do Replit ko — clean aur surgical hai. Baaki sab chhod dega. ✅